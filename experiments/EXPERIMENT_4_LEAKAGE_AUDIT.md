# Experiment 4 — leakage audit

Generated by `experiments/run_experiment4_analysis.py` on 2026-09-03 20:21 UTC.

Runs **before** any Phase C model is trained. Phase C does not execute if any check fails.

**Overall: PASS — 10 of 10 checks passed.**

## Checks

### 1. No yield-derived covariates — PASS

The target is rice yield = rice production / rice AREA. Any covariate built from rice area shares a term with the target's denominator. ['gross_cropped_area_ha', 'rice_area_share'] are therefore barred from every model and are reported in the descriptive analysis only. Covariates entering models: ['non_rice_cropped_area_ha', 'n_crops_grown', 'elevation_m_mean', 'slope_deg_mean'] - none is a yield, a production figure, or derived from either.

### 2. No future information — PASS

Agricultural covariates are matched on the SAME (state, district, year, season) key as the yield row, so they describe the same season, never a later one. Terrain covariates are static physical properties with no time index. No covariate is a forward-looking aggregate, and no value from year Y+1 reaches a row in year Y.

### 3. No test districts influencing preprocessing — PASS

In the cross-region setting the scaler and the imputation median are computed inside fit_eval() from the TRAINING frame only. Train districts (16) and test districts (12) intersect in 0 groups.

### 4. No test data used for feature selection — PASS

The covariate set was fixed by three pre-stated rules - yield-denominator overlap, >20% missingness, and the region-fingerprint screen - all evaluated on distributional and structural properties, never on test-set predictive performance. No covariate was kept or dropped because it improved a test score.

### 5. No state identifier encoded as a feature — PASS

Feature list is ['non_rice_cropped_area_ha', 'n_crops_grown', 'elevation_m_mean', 'slope_deg_mean']; none of ['state', 'is_tn'] appears. Additionally the region-fingerprint screen removed ['n_rice_seasons'] precisely because a near-perfect region separator is a state identifier in disguise.

### 6. No district identifier encoded as a feature — PASS

Feature list is ['non_rice_cropped_area_ha', 'n_crops_grown', 'elevation_m_mean', 'slope_deg_mean']; none of ['district', 'district_id', 'canonical_district_name', 'group'] appears. Additionally the region-fingerprint screen removed ['n_rice_seasons'] precisely because a near-perfect region separator is a state identifier in disguise.

### 7. Static covariates tested separately — PASS

['elevation_m_mean', 'slope_deg_mean'] are per-district constants and therefore location fingerprints of the kind Experiment 1 already caught with soil. Phase C runs a mandatory 'static covariates only' arm so that any gain from the full model can be compared against what location identity alone buys.

### 8. Train/test geographic separation maintained — PASS

Every Phase C arm evaluates AP+Telangana -> Tamil Nadu, so train and test share no district by construction; validation districts are drawn from the source region only.

### 9. Missing-value imputation is train-only — PASS

fit_eval() computes the per-column median on the TRAINING frame and applies that same median to validation and test. No test row contributes to the imputation statistic.

### 10. Scaling is train-only — PASS

StandardScaler is fit on the training matrix and applied unchanged to val and test; its `fitted_on` attribute records the split it saw.

## A hazard this audit surfaced rather than assumed

The region-fingerprint screen flagged **n_rice_seasons**. `n_rice_seasons` separates Tamil Nadu from Andhra Pradesh/Telangana almost perfectly (KS 0.996) and has zero variance within Tamil Nadu, because the source resource reports Tamil Nadu rice under a single season in these years while reporting AP/Telangana rice under two. That is a **reporting-convention difference, not an agronomic one**, and a model given it would be reading region identity, not cropping intensity. It is excluded from every model on that basis, and its strong pooled correlation with yield is reported in the main report as a between-region artefact.
