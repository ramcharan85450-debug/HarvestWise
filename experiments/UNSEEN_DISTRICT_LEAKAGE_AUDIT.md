# Unseen-district evaluation — leakage audit

Generated by `experiments/run_unseen_district_experiment.py` on 2026-09-03 18:57 UTC.

This audit runs **before** any model is trained. The experiment script refuses to train if any check below fails.

**Overall: PASS — 9 of 9 checks passed.**

## Checks

### 1. No overlapping districts between TRAIN and TEST — PASS

Checked all 5 seeds; train∩test was empty in every one.

### 2. No overlapping districts between VALIDATION and TEST — PASS

Checked all 5 seeds; val∩test was empty in every one. (train∩val was also empty in every seed.)

### 3. Scalers fit ONLY on training data — PASS

StandardScaler.fitted_on='train', fitted on 325 rows, which equals the training split's row count (325). build_split_datasets() computes the scaler from the training frame and then applies that same fitted object to val and test.

### 4. Imputers fit ONLY on training data — PASS

No imputation is performed anywhere in this pipeline, because none is needed: the 561 fully-aligned examples contain 0 missing values across all 12 features. Rows lacking any modality were excluded up front (counted in the exclusion report) rather than imputed. Vacuously satisfied.

### 5. Baseline mean computed ONLY from training labels — PASS

TrainMeanBaseline.fit() received 325 labels (= training rows) and produced 3.0964 t/ha, exactly the training-label mean. The test split's own mean is 3.0388 t/ha and is never read during fitting - the two differ, confirming the baseline is not secretly the test mean.

### 6. Test data never used for early stopping / checkpoint selection — PASS

train_model()'s signature is (train_ds, val_ds, seed, epochs, batch_size, lr, weight_decay, patience, hidden_dims, dropout) - it accepts no test dataset, so it is structurally incapable of reading one. An AST walk of its executable body (docstring excluded) finds 0 referenced name(s) containing 'test' (none), and confirms early stopping keys on `val_loss`/`best_val`. The test split is touched exactly once per (seed, configuration), by the caller, after training returns.

### 7. Target not included as a feature — PASS

Target column is 'final_yield_t_ha'. The 12 feature columns across all configurations are ['satellite_evi_mean', 'satellite_ndvi_mean', 'satellite_ndwi_mean', 'soil_clay', 'soil_nitrogen', 'soil_phh2o', 'soil_sand', 'soil_soc', 'weather_humidity_pct_mean', 'weather_precip_mm_sum', 'weather_temp_c_mean', 'weather_wind_speed_ms_mean'] - the target is not among them. training/district_dataset.assert_no_forbidden_features() raises at dataset construction if it ever were.

### 8. Source URLs / provenance strings not used as features — PASS

No feature column contains a source or URL (none found). yield_source_url, weather_source, satellite_source and soil_source are all in FORBIDDEN_AS_FEATURES. This matters concretely here: Tamil Nadu's rows cite different source URLs than Andhra Pradesh's, so a source string is a perfect region fingerprint.

### 9. District / state identifiers not used as features — PASS

No feature column is an identifier (none found). state, district, canonical_district_name and district_id are all in FORBIDDEN_AS_FEATURES and are kept as metadata only. Additionally `year` and `season` are excluded despite being ordinary columns: in this dataset every Tamil Nadu example is 'Whole Year' in 2019/2024 while every AP/Telangana example is Kharif/Rabi in 1999-2012, so either column would identify the region perfectly and defeat the unseen-district design.

## Per-seed district disjointness (the core guarantee)

| Seed | Train districts | Val districts | Test districts | train∩test | val∩test | train∩val |
|---|---|---|---|---|---|---|
| 42 | 34 | 12 | 12 | 0 | 0 | 0 |
| 43 | 34 | 12 | 12 | 0 | 0 | 0 |
| 44 | 34 | 12 | 12 | 0 | 0 | 0 |
| 45 | 34 | 12 | 12 | 0 | 0 | 0 |
| 46 | 34 | 12 | 12 | 0 | 0 | 0 |

## Feature columns actually used (all configurations combined)

```
satellite_evi_mean
satellite_ndvi_mean
satellite_ndwi_mean
soil_clay
soil_nitrogen
soil_phh2o
soil_sand
soil_soc
weather_humidity_pct_mean
weather_precip_mm_sum
weather_temp_c_mean
weather_wind_speed_ms_mean
```

12 environmental features in total. Every other column in `data/processed/district_multimodal_examples.csv` is either the target, metadata, provenance, or data-quality bookkeeping, and is listed in `FORBIDDEN_AS_FEATURES` with a stated reason in `training/district_dataset.py`'s module docstring.

## Dataset used

- Collected rows in the aligned CSV: **868**
- Used (fully aligned: real weather AND satellite AND soil): **561**
- Excluded: **307**
- Missing values remaining in the used subset's features: **0**