# Root environment for data ingestion + model training (run in Colab for GPU-backed steps).
# backend/ and frontend/ have their own lighter requirements.txt for serving only.

# data acquisition
earthengine-api>=0.1.400
cdsapi>=0.7.0
requests>=2.32

# data handling
numpy>=1.26
pandas>=2.2
xarray>=2024.6
rasterio>=1.3
geopandas>=1.0
netCDF4>=1.7

# modeling
torch>=2.3
torchvision>=0.18
timm>=1.0
stable-baselines3>=2.3
gymnasium>=0.29

# evaluation / baselines
scikit-learn>=1.5
xgboost>=2.0
scipy>=1.13
shap>=0.52
matplotlib>=3.9

# experiment tracking
wandb>=0.17

pytest>=8.2
